local sbar = require("sketchybar")
require("items.widgets.volume")
require("items.widgets.battery")

-- Ensure the padding item has an explicit position
-- sbar.add("item", "widget.padding", {
--   position = "right",
--   width = 8,
-- })
